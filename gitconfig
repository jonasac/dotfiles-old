[user]
	name = jonasac
	email = jonasac@student.matnat.uio.no
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	editor = vim
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:\"%h %ad | %s%d [%an] \" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
    subup = submodule foreach git pull origin master
